# SendGrid API Configuration
SENDGRID_API_KEY= your_sendgrid_api_key_here

# Optional: Default sender email (must be verified in SendGrid)
DEFAULT_FROM_EMAIL=your-verified-email@example.com

DEFAULT_FROM_NAME=your-preffered-name

# Optional: Default template settings
DEFAULT_TEMPLATE_ID=your-template-id

# Optional: Rate limiting (requests per second)
RATE_LIMIT=10

# Optional: Debug mode
DEBUG=false
